# Rocq Coursework

The task was to prove certain programs run in a simple implementation of the JVM inside rocq. 

> Using the provided Rocq code template that formalises the Java Virtual Machine (JVM) and its instructions, complete the missing code in Rocq.

It's proven to be true because it type checks. That's pretty cool.



